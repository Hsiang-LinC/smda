[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "smda-scheduler"
version = "0.1.0"
description = "State-machine-driven automation scheduler for agentic software development"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
smda = "smda_scheduler.cli:main"
smda-scheduler = "smda_scheduler.cli:main"

[tool.setuptools]
package-dir = {"" = "packages/scheduler/src"}

[tool.setuptools.packages.find]
where = ["packages/scheduler/src"]

[tool.setuptools.package-data]
# Ship the generated, zod-canonical schema artifact (ADR-0005) in the wheel.
smda_scheduler = ["schemas/*.json"]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["packages/scheduler/tests"]
pythonpath = ["packages/scheduler/src"]
